import { AppRegistry } from 'react-native';
import App from './src/App';

// TEMP: Remove for testing and production.
console.disableYellowBox = true;

AppRegistry.registerComponent('raveltree', () => App);

// TEMP: Remove for testing and production.
console.disableYellowBox = true;
